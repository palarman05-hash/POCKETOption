import React,{useState} from 'react';const API=import.meta.env.VITE_API_URL||'http://localhost:4000';export default function App(){const [res,setRes]=useState(null);const run=async()=>{const r=await fetch(API+'/predict',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({pair:'OTC-EURUSD',tf:'1m'})});setRes(await r.json());};return(<div style={{padding:20}}><h1>Finorix v9 (Demo)</h1><button onClick={run}>Get Signal</button>{res&&<pre>{JSON.stringify(res,null,2)}</pre>}</div>);} 